# 本文件是模板。真正的凭据在同目录的 secrets.env 里，由配置页写入，不进版本库。
#
# 正常路径不需要手工碰任何一个文件：
#     浏览器打开 http://localhost:3789 → 粘贴令牌 → 点「保存并生效」
# 配置页会原子写入 secrets.env、重启 TAPD 子服务、注册进 DeepTutor、回读状态。
#
# 手工编辑只在排障时才需要（比如配置页起不来）。格式是 KEY=VALUE，一行一条，
# '#' 开头是注释，值里的 '=' 会原样保留（只按第一个 '=' 切）。改完重启网关生效。
#
# 权限的实话：Windows 上这就是一个普通文件，同机器上以你身份运行的任何程序都读得到。
# 我们做到的是「不进版本库、不出本机、不进聊天、不进容器、不进日志」，
# 做不到的是操作系统级的凭据隔离（那要上 DPAPI / 凭据管理器，后续再说）。

# ── TAPD ────────────────────────────────────────────────────────────────────
# 个人访问令牌。TAPD 网页端右上角头像 → 个人设置 → 个人访问令牌 → 生成，
# 只勾选你自己需要的项目。禁用 TAPD_API_USER / TAPD_API_PASSWORD：
# 那条路是 Basic Auth，base64 可逆，等于明文托管账号密码（审计条件 B）。
#
# 注意：TAPD_API_BASE_URL 不在本文件里，也不该在——它被网关钉死成
# https://api.tapd.cn（server/gateway/supervisor.py 的 PINNED_API_BASE_URL）。
# 官方包对 base_url 无任何校验，那一行是凭据流向的唯一闸门，不参数化。
TAPD_ACCESS_TOKEN=

# ── 可选 ────────────────────────────────────────────────────────────────────
# 企业微信机器人 webhook，只用于 mcp-server-tapd 的 send_qiwei_message 工具，
# 不携带 TAPD 凭据。非必要不配——多一个外发目标就多一条数据出口。
# 变量名是 BOT_URL，不是 TAPD_BOT_URL；写错了不报错，只会静默失效。
# BOT_URL=

# DeepTutor 的 admin 令牌。只有你的 DeepTutor 开了登录（AUTH_ENABLED=true）才需要——
# 本地默认部署下配置接口不校验身份，留空即可。填了会以 Authorization: Bearer 发出，
# 且只发给 config/gateway.json 里 deeptutor.api_base 指向的本机地址。
# DEEPTUTOR_TOKEN=
