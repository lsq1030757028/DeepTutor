# 0020 · 上游触点预算提到 11 + 不做 capability 配置卡

- 日期：2026-08-11
- 决策人：用户（两项均为用户拍板）
- 关联：护栏 6（HANDOFF v2，原定 ≤5 处）、0019（载体=正式 Capability）

## 裁定一 · 上游触点预算 5 → 11，用户接受超支

护栏 6 原定动 DT 主体 ≤5 处，超出须上浮拍板。按 0019 的正式 Capability 载体，实际需 **9~11 处**（工作台侧 4 + 聊天侧 5~7）。用户选中「接受，预算提到 11」。

**manager 失误如实记录**：0019 起草时，manager 只把聊天侧 5~7 处报为"知情接受"，**未把工作台侧已在 0017 登记的 4 处合并计入总数**，导致用户在批准 0019 时未见到真实总量。总量超护栏一倍属护栏 6 明列的必须上浮项，不应由 manager 吞下。本次已补上浮并由用户拍板。

**附带要求**（manager 自决，随裁定执行）：
- 触点集中成可整块删除的 fork 区块（与 0018 对 Dockerfile 的同类要求一致）。
- **首次同上游时实测冲突面并回报**；若"形态轻、冲突小"的判断被证伪，重估 B 的长期成本（0019 撤回信号）。
- 登记表原因栏按 0019 修正条如实写明哪些是"改渲染逻辑"，禁美化。

## 裁定二 · 不做 capability 配置卡，档位与靶机改用人闸卡问

用户选中「改用卡片问，不做配置卡」。预算维持 11，不增至 16~17。

**决策依据被修正过一次，如实记录**：
- manager 第一次征询时告知"配置卡占 1 处上游改动"——**该数字错误**，来自交互稿的估算。
- 工程岗独立算出 6 处；manager 亲自核代码确认工程岗对：`web/components/chat/home/CapabilityConfigCard.tsx:31-44` 的能力名是**硬编码联合类型 + 硬编码标签表**，非数据驱动；接入还需改 `ChatComposer.tsx`、两个页面、服务端 `request_contracts.py` 与 `turn_runtime.py`。
- manager 带着错误成本征询过用户一次，已主动撤回该轮结论并重问。**用户第一次的选择（要配置卡）作废，以本次为准。**

**架构侧支持该结论**：配置卡本质是"向导的第一步"，与 HANDOFF §三 架构形态约束「原子工具 + 类型化产物接口，禁向导式流水线；任意前缀是合法交付」冲突。用卡片在读完需求后问，还能带上"这条需求有 N 条规则"的上下文，问得更准。

## 附带结论 · capability 跨轮保持已实证（风险解除）

0019 曾把"模式选中后是否跨轮保持"标为 `[推断未实证]`，并列为 B 入口体验的成败关键。m2-design 已实证成立（三条独立证据，全量无截断枚举：`SET_CAPABILITY` 全仓 3 处、`setCapability(` 全仓 1 处即 picker handler、`handleSend` 不清空；服务端 `turn_runtime.py:754-755` 写 preferences、`:848` 回灌）。

- 结论：多轮旅程无需反复选模式，**B 的入口体验成立**。
- 交互稿 §6d 的"粘滞提示条"降级形态**不实现**，保留为文档形态以备证伪。
- 残留：运行态冒烟列 Day-1 非阻塞项。

## 施工手法报备（manager 自决，无需拍板）

- m2-design 的 §13.3 B 档策略：把上游测试清单那处触点改为独立新增测试文件（只断言 `"test" in BUILTIN_CAPABILITY_CLASSES`），登记行 6→5，且消掉的正是唯一"保证会冲突"的一条。
- 坑 1（partner 人闸静默降级）取案甲：`extensions/` 内收窄 `builtin_tools` + 改两份 SKILL.md，0 触点治根因；否决"加提示文案"的案乙（属"加文案当修复"）。
