"""用例生成链路（工作台第 3-5 屏）。

本包的硬约束：**零 DeepTutor import**。

理由是分层——提示词、上下文裁剪、输出解析、阶段机、任务台账都是纯逻辑，
不该知道模型是谁调的。模型调用由宿主侧的 router 注入一个可调用对象进来
（签名见 `model.CallModel`），于是：

- 本包可以脱离 DeepTutor 单独跑测试（现有 670+ 例的跑法一行不改）
- `extensions/test-partner` 仍能作为宿主机 MCP 服务器独立运行，那条线不断
- 宿主侧只有一个文件懂模型，审查面收敛到一处

对应决策：0010（设计定稿）、0011（复用平台机制，不自建）。
"""
