# 一般不需要手工碰这个文件：双击 setup_tapd.cmd 粘贴令牌，.env 由它生成（CRLF 保证正确）。
# 本文件是手工模式（排障用）的模板：复制为 .env 后填值。
# .env 已被 tapd-runtime/.gitignore 拦住，不会进库。
# 采用条件出处：docs/research/mcp-server-tapd-audit-20260804.md 第 B 条。

# 个人令牌，在 TAPD 个人设置 -> 个人访问令牌 生成，只授权自己需要的项目。
# 禁用 TAPD_API_USER / TAPD_API_PASSWORD：那条路是 Basic Auth，base64 可逆，等于明文托管账号密码。
TAPD_ACCESS_TOKEN=

# 钉死官方域名。代码对 base_url 无协议/域名校验（审计报告"弱点"段），
# 凭据发往哪台机器完全由这一行决定 —— 这是凭据流向的唯一闸门，不要改、不要参数化。
TAPD_API_BASE_URL=https://api.tapd.cn

# 可选：企业微信机器人 webhook，只用于 send_qiwei_message 工具，不携带 TAPD 凭据。
# 非必要不配 —— 多一个外发目标就多一条数据出口。留空/删掉即禁用该工具的实际投递。
# 注意变量名：源码 app_config.py 读的是 BOT_URL，不是 TAPD_BOT_URL，写错了不会报错、只会静默失效。
# BOT_URL=
